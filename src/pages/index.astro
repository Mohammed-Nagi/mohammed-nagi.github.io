---
import BaseLayout from '../layouts/BaseLayout.astro';
import Section from '../components/Section.astro';

const GITHUB_URL = 'https://github.com/Mohammed-Nagi';
const LINKEDIN_URL = 'https://www.linkedin.com/in/mohammed-nagi-418924236/';
const EMAIL = 'mo.nagi017@gmail.com';

const projects = [
	{
		title: 'PsychRoute',
		description: 'A LinUCB contextual bandit over a fine-tuned MentalBERT classifier, routing psychiatric triage cases between model and clinician.',
		repo: `${GITHUB_URL}/psychroute`,
	},
	{
		title: 'Recommender System',
		description: 'A hardware-aware alternating least squares recommender system on MovieLens 32M.',
		repo: `${GITHUB_URL}/MLAS`,
	},
	{
		title: 'SemEval-2026 Task 9',
		description: 'Two-stage multilingual polarisation detection using Focal Loss and dynamic per-class thresholding on DeBERTa and MARBERTv2 backbones.',
		repo: `${GITHUB_URL}/NLP`,
	},
	{
		title: 'Spectral graph theory',
		description: 'The Cross-Entropy Method extended with graph neural network policies, searching combinatorial spaces up to N=35 for counterexamples to open conjectures.',
		repo: null,
	},
];

const personJsonLd = {
	'@context': 'https://schema.org',
	'@type': 'Person',
	name: 'Mohammed Nagi',
	jobTitle: 'AI Researcher',
	affiliation: {
		'@type': 'Organization',
		name: 'AIMS South Africa',
	},
	sameAs: [GITHUB_URL, LINKEDIN_URL],
};
---

<BaseLayout title="Mohammed Nagi" description="Mohammed Nagi — AI Safety researcher specialising in mechanistic interpretability.">
	<script type="application/ld+json" set:html={JSON.stringify(personJsonLd)} />
	<div class="home-layout">
		<aside class="identity">
			<img
				src="/images/identity.jpg"
				width="480"
				height="480"
				alt="Mohammed Nagi"
				class="identity-photo"
			/>
			<h1>Mohammed Nagi</h1>
			<p class="tagline">MSc Artificial Intelligence, AIMS.<br />Google DeepMind Scholar.</p>
			<ul class="identity-links">
				<li>
					<a href={`mailto:${EMAIL}`} aria-label="Email">
						<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
							<rect x="3" y="5" width="18" height="14" rx="2" />
							<path d="m3 7 9 6 9-6" />
						</svg>
						<span>Email</span>
					</a>
				</li>
				<li>
					<a href={GITHUB_URL} aria-label="GitHub">
						<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
							<path d="M9 19c-4.3 1.4-4.3-2.5-6-3m12 5v-3.5c0-1 .1-1.4-.5-2 2.8-.3 5.5-1.4 5.5-6a4.6 4.6 0 0 0-1.3-3.2 4.2 4.2 0 0 0-.1-3.2s-1.1-.3-3.5 1.3a12.3 12.3 0 0 0-6.2 0C6.5 2.8 5.4 3.1 5.4 3.1a4.2 4.2 0 0 0-.1 3.2A4.6 4.6 0 0 0 4 9.5c0 4.6 2.7 5.7 5.5 6-.6.6-.6 1.2-.5 2V21" />
						</svg>
						<span>GitHub</span>
					</a>
				</li>
				<li>
					<a href={LINKEDIN_URL} aria-label="LinkedIn">
						<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
							<rect x="3" y="3" width="18" height="18" rx="2" />
							<path d="M7 10v7M7 7v.01M12 17v-4.5a2.5 2.5 0 0 1 5 0V17m-5-4.5V17" />
						</svg>
						<span>LinkedIn</span>
					</a>
				</li>
				<li>
					<a href="/cv.pdf" download aria-label="Download CV">
						<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
							<path d="M12 3v12m0 0-4-4m4 4 4-4M4 17v2a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-2" />
						</svg>
						<span>CV</span>
					</a>
				</li>
			</ul>
		</aside>

		<div class="content">
			<p class="summary" id="about">I work on <strong>mechanistic interpretability</strong>, with a focus on models that encode physical signals
			rather than language. Across my work, I prioritise claims that can be <strong>falsified by intervention</strong> and I am motivated by the broader question
			of what studying intelligence in <strong>non-linguistic systems</strong> can teach us about interpretability methods built primarily on language models.</p>

			<ul class="interests" aria-label="Interests">
				<li>AI Safety</li>
				<li>Human-AI Collaboration</li>
				<li>Causal Inference</li>
				<li>Photography</li>
			</ul>

			<Section id="research" title="Research">
				<article class="entry">
					<h3><a href={`${GITHUB_URL}/instanovo-sae`}>Sparse Autoencoders on InstaNovo</a></h3>
					<p>Sparse autoencoder dictionaries trained on the residual stream activations of InstaNovo, asking which learned features correspond to which fragmentation events.</p>
				</article>
			</Section>

			<Section id="projects" title="Projects">
				<ul class="project-list">
					{
						projects.map((project) => (
							<li class="entry">
								<h3>
									{project.repo ? <a href={project.repo}>{project.title}</a> : project.title}
								</h3>
								<p>{project.description}</p>
							</li>
						))
					}
				</ul>
			</Section>
		</div>
	</div>
</BaseLayout>

<style>
	.home-layout {
		display: flex;
		flex-direction: column;
		gap: 4rem;
		max-width: 1300px;
		margin-inline: auto;
	}

	@media (min-width: 900px) {
		.home-layout {
			flex-direction: row;
			align-items: flex-start;
		}
	}

	.identity {
		flex: 0 0 480px;
		text-align: center;
		padding: 2rem;
		background: var(--surface);
		border: 1px solid var(--rule);
		border-radius: 0.5rem;
	}

	@media (min-width: 900px) {
		.identity {
			position: sticky;
			top: 6rem;
		}
	}

	.identity-photo {
		width: 100%;
		max-width: 220px;
		height: auto;
		aspect-ratio: 1 / 1;
		object-fit: cover;
		background: var(--surface);
		border-radius: 50%;
		margin-inline: auto;
	}

	.identity h1 {
		font-size: var(--step-3);
		margin-block: 1.75rem 0.25rem;
	}

	.tagline {
		color: var(--text-muted);
		font-size: var(--step--1);
		margin: 0 0 2rem;
	}

	.identity-links {
		list-style: none;
		margin: 0;
		padding: 0;
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}

	.identity-links a {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 0.6rem;
		color: var(--accent);
		text-decoration: none;
		font-size: var(--step--1);
	}

	.identity-links a:hover {
		color: var(--accent-hov);
	}

	.content {
		flex: 1 1 auto;
		min-width: 0;
		max-width: var(--measure);
	}

	.summary {
		scroll-margin-top: calc(var(--header-h) + 1rem);
		font-family: var(--font-serif);
		font-optical-sizing: auto;
		font-size: clamp(1.0625rem, 1rem + 0.25vw, 1.2rem);
		line-height: var(--leading-prose);
		margin-block: 1rem 2.5rem;
	}

	.interests {
		list-style: none;
		margin: 0;
		padding: 0;
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
	}

	.interests li {
		font-size: var(--step--1);
		color: var(--text-muted);
		border: 1px solid var(--rule);
		border-radius: 0.375rem;
		padding: 0.35rem 0.85rem;
	}

	.project-list {
		list-style: none;
		margin: 0;
		padding: 0;
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}

	.project-list h3,
	article h3 {
		margin-block: 0 0.25rem;
		font-size: var(--step-1);
	}

	.project-list h3 {
		color: var(--accent);
	}

	.project-list h3 a,
	article h3 a {
		text-decoration: none;
	}

	.project-list h3 a {
		color: inherit;
	}

	.project-list h3 a:hover,
	article h3 a:hover {
		text-decoration: underline;
	}

	.project-list p,
	article p {
		margin: 0;
		font-size: var(--step--1);
		color: var(--text-muted);
	}

	.entry {
		position: relative;
		padding: 0.75rem 1rem;
		margin: -0.75rem -1rem;
		border-radius: 0.375rem;
		transition: background-color 150ms ease;
	}

	.entry:hover,
	.entry:focus-within {
		background: var(--surface);
	}

	.entry h3 a::after {
		content: '';
		position: absolute;
		inset: 0;
	}
</style>
